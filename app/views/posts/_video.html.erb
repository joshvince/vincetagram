<%= video_tag(
      rails_blob_path(@post.video),
      class: 'object-contain h-full w-full mt-3 mb-4',
      controls: true,
      data: { action: 'loadedmetadata->post#displayPost', post_target: 'media' }
    ) %>
